stages:
  - build
  - deploy

variables:
  APPLICATION_NAME: cms-front-nuxt

  CONTAINER_REGISTRY: ${DOCKER_HUB_USERNAME}
  CONTAINER_IMAGE: ${CONTAINER_REGISTRY}/${APPLICATION_NAME}
  CONTAINER_TAG: ${CI_COMMIT_SHORT_SHA}
  CONTAINER_IMAGE_TAGGED: ${CONTAINER_IMAGE}:${CONTAINER_TAG}

  K8S_INGRESS_HOST: nuxt.cms.maximemoreillon.com


image: moreillon/ci-dind
services:
  - name: docker:19.03.12-dind


build:
  stage: build
  tags:
    - dind
  before_script:
    # Docker hub login
    - echo "${DOCKER_HUB_PASSWORD}" | docker login --username ${DOCKER_HUB_USERNAME} --password-stdin
  script:
    # Building the container image
    - docker build -t ${CONTAINER_IMAGE_TAGGED} .
    # Pushing
    - docker push ${CONTAINER_IMAGE_TAGGED}


deploy:
  stage: deploy
  tags:
    - dind
  script:
    - envsubst < kubernetes_manifest.yml | kubectl apply -f -
  environment:
    name: production
    kubernetes:
      namespace: ${KUBERNETES_NAMESPACE}
